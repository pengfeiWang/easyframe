utils =  {
	globalCache : globalCache,
	browser     : _browser,
	isArray     : _isArray,
	unique      : _unique,
	each        : _each,
	getType     : _getType,
	extend      : _extend,
	getEle      : _getElement,
	getElement  : _getElement,
	css         : _css,
	hasClass    : _hasClass,
	addClass    : _addClass,
	removeClass : _removeClass,
	on          : _on,
	off         : _off,
	one         : _one,
	trigger     : _trigger
}
window.utils = utils;
return utils;
